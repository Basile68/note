# 计算机的三大原则

## 热身问答

硬件物理上存在，软件物理上不存在  
编码是计算机给信息赋予的数字

## 计算机三原则

1. 计算机时执行**输入**、**运算**、**输出**的机器
2. 程序是指令和数据的集合
3. 机器有自己的逻辑

## 输入、运算、输出是硬件的基础

信息输入运算输出，一套流程缺一不可

* 软件是指令和数据的集合
    * 指令即对计算机发出的命令，指令有对应的一或多的名字
* 计算机的逻辑中一切都是数字来表达
* 指令和数据有它们自己的规范

---
计算机主要是由IC组成，有三种IC：CPU、内存以及I/O（链接主机与其他设备）
==时钟信号==

# 基础知识

## 热身问答

CPU-Central Processing Unit负责解释并执行程序的内容  
Hz频率单位，一秒发出一次时钟信号就是1Hz（时钟信号是什么  
Z80 CPU是8比特的CPU

- 比特数=CPU上数据总线的条数/CPU内部参与运算的寄存器的容量

## 时钟信号

- 电子系统中信号是指带有信息的电压或者电流变化
- 每当时钟信号到达特定边沿，电路就会执行一次操作
- 时钟信号就好像带有一个时钟，每隔一定时间就变换一次电压的高低

## 基础知识

1个2进制数（也就是数字IC上1个引脚所能表示的0或者1）所表示的信息称作1比特  
8个2进制数也就是8比特称作1字节  
数字IC的“数字”是指它处理的是离散的二进制信号，而非连续模拟信号（连续模拟信号是指信号的幅值可以在一定范围内连续变化，没有离散的跳跃

# 手工汇编

## 热身问答

机器语言是二进制数字构成的程序，CPU可以直接对其解释、执行。汇编语言及用编程语言写的程序都要转换成机器语言才能被执行，机器语言优势也被叫做原生代码（Native
Code）

# 程序的流程

## 热身问答

flow chart流程图是指用图的形式表示程序的流程
程序流程分三种：顺序执行、条件分支、循环
事件驱动-根据事件的类型做出相应的处理

## 程序的流程分为三种

硬件层面上计算机的运作方式：内存储存程序，CPU配合时钟信号从内存中读取指令，然后再依次对其进行解释和执行

## 画流程图来思考算法

算法是为了解决一个问题而设置的一套清晰的、可执行的步骤
思考算法时的要点是要分两步走：

- 从整体上考虑程序的粗略流程
- 再考虑程序各个部分细节的流程

## 特殊的程序流程-中断处理&事件驱动

中断处理是指犹豫外部原因使原本正常的流程终端，中断后再返回到之前流程的过程
事件驱动是指根据事件的类型做出相应的处理

# 小流程的算法

## 热身问答

算法Algorithm
辗转相除法是计算最大公约数的算法，最大公约数是指两个整数的公共约数中最大的数
“哨兵”是指一种含有特殊值的数据，可以用于标识数据的结尾等。字符串的末尾用0表示，链表的末尾用-1表示，像这种特殊的数据就是哨兵

## 机器不能自发地思考

计算机所执行的由程序表示的算法必须是由机械的步骤所构成
典型算法 = 解决某类问题的“标准例子 / 经典代表”

## 了解并应用典型算法

## 使用编程技巧提升程序执行速度

哨兵

# 数据结构

## 热身问答

程序中的变量是指按照变量所储存数据的大小被分配到的一块内存空间，  
把若干个数据沿直线排列起来的数据结构叫做数组，数组的实质是连续分配的一块特定大小的内存空间，    
栈中数据的存取形式是LIFO（last in first out 后进先出）表示优先读取后存入的数据，
队列中数据的存取形式是FIFO（ first in first out 先进先出）表示优先读取先存入的数据。
---

> 本章主题：如何结合计算机的特性，用程序来表示现实世界中的数据结构

---

## 了解内存和变量的关系

对于程序员来说，他们并不需要知道变量被存储到内存空间中的哪个地址上去了，因为当程序运行时是由操作系统为我们从尚未使用的内存空间中划分出一部分分给变量的

## 数组

数组是数据结构的基础，之所以这么说是因为数组反应了内存的物理结构本身  
在内存中存储数据的空间是连续分布的  
在程序中，往往要从内存整体中分配出一块连续的空间以供使用

## 数组的应用

数组是数据结构的基础  
数组是一种直接利用内存物理结构（计算机的特性）的最基本的数据结构  
主要的典型数据结构：

- 栈
    - 把数据像小山一样堆积起来
- 队列
    - 把数据排成一队
- 链表
    - 可以任意地改变数据的排列顺序
- 二叉树
    - 把数据分为两路排列

## 栈

通过使用由数组、栈顶指针以及入栈函数和出栈函数所构成的集合，就能实现栈这种数据结构了。  
应用场景：

- 打开网页，点链接，再点链接……然后按“返回”
- 你点了：百度 → 进入新闻 → 点了一条体育新闻 你现在在第3页。
- 当你点“返回”：先回到“新闻”（第2页）再“返回”，回到“百度”（第1页）
- 就像从一个盒子一张张拿出纸条，顺序是反过来的。
- 浏览器用“栈”记住你去过哪里

栈 = 一个任意大小的数组 + 入栈函数(push) + 出栈函数(pop) + 先入后出

## 队列

~~队列 = 一个任意大小的数组 + 一个用于存放排在对头的数据对应的索引的变量 + 一个用于存放排在队尾的数据对应的变量 +
一对函数（分别用于把数据存入到队列中和从队列中把数据提取出来~~

队列 = 一个任意大小的数组 + 入队函数(add/offer/enqueue) + 出队函数(poll/dequeue) + 先入先出

## 结构体

结构体就是把若干个数据项汇集到一处并赋予其名字后所形成的整体

# 面向对象编程

## 热身问答

object 对象  
OOP（object oriented programming 面向对象编程  
C++中++表示自增（每次只将变量的值增加1）的C语言运算符  
C++之所以命名为C++，是因为C++在C语言的基础上增加了面向对象的机制这一点

## 面向对象编程

OOP主要的编程技巧有三种：继承、封装、多态  
- 继承 *inheritance*
    - 通过继承已存在的类所拥有的成员而生成新的类
- 封装 *encapsulation*
    - 在类所拥有的成员中，隐藏掉那些没有必要展现给该类调用者的成员
- 多态 *polymorphism*
    - 是针对同一种消息，不同的对象可以进行不同的操作
> 只要去继承已存在的类，就能高效地生成新的类。如果一个类被多个类所继承，那么只要修正了这个类，就相当于把继承了这个类的所有类都修正了。  
  只要通过封装把外界不关心的成员隐藏起来，类就可以被当作是黑盒，变成了易于使用且便于维护的组件了。  
  而且由于隐藏起来的成员不能被外界所访问，所以也就可以放心地随意修改这些成员。  
  只要利用了多态，生成对同一个消息可以执行多种操作的一组类，使用这组类的程序员所需要记忆的东西就减少了。  
 
在面向对象编程中，使用了一种称为“类”的要素，通过把若干个类组装到一起构建一个完整的程序，  
在使用面向对象编程语言开发时，并不是所有的类都必须由程序员亲自编写，大部分的类都已内置于面向对象编程语言中了， 
> 虽然 OOP 是一种方法，但有些编程语言在设计时就原生支持并鼓励使用面向对象的方式编程，这些语言被称为 “面向对象编程语言  

关于类的工作分为两种：制作类及使用类  
对于创造类的程序员，需要考虑的是程序的开发效率和可维护性，并决定应该将什么抽象为类  

> 在人的直觉中，大件物品都是由组件组装起来的，因此可以说面向对象编程方法把同样的直觉带给了计算机，创造了一种顺应人类思维习惯的先进的开发方法  
  在面向对象编程中，可以通过“这个是由什么样的对象构成的呢？”这样的观点来分析即将转换成程序的现实世界。这种分析过程叫作“建模”  
  建模的过程中，要进行组件化和省略化两步  
   - 组件化
     - 将可看作是由若干种对象构成的集合的现实世界分割成组件
   - 省略化
     - 省略掉不必要的部分
     
## OOP难懂的部分

### 面向对象编程通过在对象间传递消息驱动程序

使用面向对象编程语言编写程序的话，程序可以通过由一个对象去调用另一个对象所拥有的函数这种方式运行起来，这种调用方式被称为对象间的“消息传递”。  

### 多态  

多态指的是针对同一种消息，不同的对象可以进行不同的操作。  
> 多态 = 同一个操作，多种响应。就像按下“播放”键，音乐文件播放声音，视频文件开始画面，图片文件可能打不开——但都是“播放”。  

### 类和对象的区别

类是对象的定义，而对象是类的实例

### 类的三种使用方法

1. 仅调用类所持有的个别成员（函数和变量）  
2. 在类的定义中包含其他的类（这种方法被称作组合）  
3. 通过继承已存在的类定义出新的类  


# 数据库

## 热身问答

数据库术语中的“表（Table）”是指被整理成表格形式的数据，由若干个列和行构成，列被称为字段（Field），行被称为记录（Record）  
DBMS全称Database Management System（数据库管理系统），有SQL Server、Oracle、DB2等，均可以使用基本相同的SQL语句操作    
键和索引的区别是什么：键用于设定表和表之间的关系，而索引是提升数据检索速度的机制  

> SQL语句是“结构化查询语言”（Structured Query Language）的简称，它是用来操作和管理数据库的一种专用编程语言。
  高级问题：“其上每个值都能够唯一标识一条记录的字段称为主键。为了在表和表之间建立关系而在表中添加的、其他表主键的字段称为外键。而索引是与键无关的机制。”（晕）

## 数据库类型

卡片型数据库适用于想要实现小规模的数据库的情况。  
适合存储大规模数据的是关系型数据库（Relational Database）。在关系型数据库中，数据被拆分整理到多张表中，同时表与表之间的关系也可以被记录下来。  
现在关系型数据库被广泛应用，以至于一提到数据库就默认是关系型数据库。  

## 数据文件、DBMS 和数据库应用程序

数据库系统的构成要素包括：“数据文件”“DBMS”“应用程序”  

DBMS是应用程序和数据文件的中介  

> 应用程序——（请求操作数据）——>DBMS——（实际操作数据）——>数据文件
- 在小型系统中，把三个要素全部部署在一台计算机上，称作“独立型系统”。  
- 在中型系统中，把数据文件部署在一台计算机上，并且使数据文件被部署了DBMS 和应用程序的多台计算机共享，这样的系统被称为“文件共享型系统”。  
- 在大型系统中，把数据文件和DBMS 部署在一台（或者多台）计算机上，然后用户从另外一些部署着应用程序的计算机上访问，这样的系统被称作“客户端／服务器型系统”。  
   - 其中部署着数据文件和DBMS 的计算机是服务器（Server），即服务的提供者；部署着应用程序的计算机是客户端（Client），即服务的使用者。
   - 如果把服务器和客户端之间用互联网联结起来，就形成了Web 系统。在Web 系统中，一般情况下应用程序也是部署在服务器中的，在客户端只部署Web 浏览器
   
## 设计数据库
1. 从“你想要了解什么”的视角出发找出需要的数据；  
2. 要考虑的是各种数据的属性；  
   - 属性也称作模式（内模式），具体来说就是数据的类型（是数字还是字符串），数字的话是整数还是浮点小数，字符串的话最多允许包含多少个字符，是否允许NULL 值（表示未知或者不存在的值），等等。
3. 做出带有用户界面并且能够读写数据的应用程序；
   - 可以通过“规范化”将一张大表分割成多张小表，然后再在小表之间建立关系，以此来打成整理数据库结构的目的
      - 规范化的要点是在一个数据库中要避免重复储存相同的数据  
4. 用主键和外键在表间建立关系；
5. 在表的各个字段上设置索引提升数据的检索速度；
   - 索引表是一种数据结构，存储着字段的值以及字段所对应记录的位置
6. 设计用户界面。

### 一些术语

在关系型数据库中，把录入到表中的每一行数据都称为记录，把构成一条记录中的各个数据项（在本例中是商品名称、单价等）所在的列都称作字段。  

记录有时也被称为行或元组（Tuple），字段有时也被称为列或属性（Attribute）  

- 记录 = 行 = 元组 = 表中的一整行数据 → 描述一个实体的全部信息
- 元组（Tuple）来自数学和关系模型理论，是学术术语，意思就是“一组有序的数据”

- 字段 = 列 = 属性 → 描述所有实体的某一个共同特征
- 一个字段 = 表中的一个列标题及其下面的所有数据

## SQL语句

SQL 语言的标准是由ISO（International Organization for Standardization，国际标准化组织）制订的。现在市面上几乎所有的DBMS 都支持SQL 语言。  

一旦向DBMS 发送了一条命令（SQL 语句），与此相应的操作就会立刻被执行。与BASIC 或C 语言等编程语言不同的是，使用SQL 语言通常不需要定义变量或者考虑程序的执行流程。  

在Windows 应用程序中，向DBMS 发送SQL 语句时，一般情况下使用的都是被称为数据对象（Data Object）的软件组件.  

### DBMS之事务控制

事务由若干条 SQL 语句构成，表示对数据库一系列相关操作的集合。  

“事务控制”是指在数据库管理系统中，对事务的执行过程进行管理和控制，以确保数据的一致性、完整性和可靠性。  

# TCP/IP网络

## 热身问答

LAN 是指 Local Area Network（局域网），同时 WAN 是指（Wide Area Network，广域网）  
TPC/IP 是指 Transmission Control Protocol/Internet Protocol（传输控制协议和网际协议）  
   - TCP/IP 协议族是互联网所使用的一套标准协议。TCP/IP 这个名字意味着同时使用了 TCP 协议和 IP 协议
   - 对信息发送方式的规定或约束就称为“协议”（Protocol）
MAC 地址是指能够标识网卡的编号  
   - 几乎所有的网卡都会在上市前被分配一个不可变更的 MAC 地址

## LAN 和 WAN

“集线器”（Hub）是负责把各台计算机的网线相互连接在一起的集线设备。  
“路由器”（Router）是负责把公司内的网络和互联网连接起来的设备。  
通常把像这样部署在一间办公室内的小规模网络称作LAN ；把像互联网那样将企业和企业联结起来的大规模网络称作WAN。路由器负责将LAN 连接到WAN 上。  
路由器的一端会先连接到互联网服务提供商的路由器上。而在服务提供商（Provider）那里，又会继续将它们的路由器连接到其他路由器上，通过这种方式最终接入到互联网的主干线缆上。

## 网卡的 MAC 地址

在每一块网卡所带有的ROM（Read Only Memory，只读存储器）中，都预先烧录了一个唯一的 MAC （Media Access Control）地址。  
MAC 地址是由制造厂商的编号和产品编号两部分组成的，所以世界上的每一个 MAC 地址都是独一无二的。  
例： 用“-”分隔的 6 个十六进制数（每个数占 8 比特）00-00-5D-B8-39-B0 就是 MAC 地址。其中 00-00-5D 代表制造商，B8-39-B0 代表产品的编号。

## 网络相关知识

和计算机一样，网络也是硬件和软件的集合体。  

### LAN 用到的硬件
在组建公司内部的网络时，笔者购买了如下 4 种硬件：
1. 安装到每台计算机上的网卡（NIC，Network Interface Card）；
2. 插到网卡上的网线；
3. 把网线汇集起来连接到一处的集线器；
4. 用于接入到互联网的路由器。

> 需要注意的是这些硬件的规格只有相互匹配了才能连接在一起。

### CSMA/CD
以太网的机制- CSMA/CD（Career Sense Multiple Access with Collision Detection，带冲突检测的载波监听多路访问）
  - 在小规模的LAN 中，像这样略显粗躁的CSMA/CD 机制是可以正常运转的。因为CSMA/CD 归根结底也只是一种适用于LAN 的机制。
    1. 载波监听（Career Sense），指的是这套机制会去监听（Sense）表示网络是否正在使用的电信号（Career）。
    2. 而多路复用（Multiple Access）指的是多个（Multiple）设备可以同时访问（Access）传输介质。
    3. 带冲突检测（with Collision Detection）则表示这套机制会去检测（Detection）因同一时刻的传输而导致的电信号冲突（Collision）。
即以太网中的每台计算机都需要先确认一件事：在网线上有没有其他的计算机正在传输电信号，也就是说要先确保没有人在占用网络，然后才能发送自己想传输的电信号。谁先抢到了网线的使用权，谁就先发送。  
万一遇到了多台计算机同时都想发送电信号的情况，只需要让这些计算机等待一段长度随机的时间后再重新发送相同的电信号即可。  

### 以太网中通过 MAC 地址的信号接收
在以太网中，发送给一台计算机的电信号也可以被其他所有的计算机收到。  
一台计算机收到了电信号以后会先做判断，如果是发送给自己的则选择接收，反之则选择忽略。  
可以用被称作MAC（Media Access Control）地址的编号来指定电信号的接收者。

## 计算机的 IP 地址

### 为什么要有 IP 地址
假如在互联网中只能使用 MAC 地址，那在接入互联网的数量众多的计算机中，只有尚未进行任何分组处理的编号（ MAC 地址）。这样的话，仅仅是寻找信息的发送目的地就要花费大量的时间。  
因此，在 TCP/IP 网络中，除了硬件上的 MAC 地址，还需要为每台计算机设定一个软件上的编号。这个编号就是众所周知的 IP 地址。  

> 通常把设定了 IP 地址的计算机称为“主机”（Host）。因为路由器也算是计算机的一种，所以它们也有 IP 地址。  
  在 TCP/IP 网络中，传输的数据都会携带 MAC 地址和 IP 地址两个地址。

IP 地址是一个 32 比特的整数，每 8 比特为一组，组间用“.”分隔， 分成 4 段表示。  
8 比特所表示的整数换算成十进制后范围是0～255，因此可用作 IP 地址的整数是0.0.0.0～255.255.255.255
   - 1 比特（bit） 是信息的最小单位，是“二进制位”（binary digit）的简称。
   - 1 比特只能表示两种状态：0 或 1
   - 1 比特能表示的状态数是2种（0，1），2 比特能表示的状态数是4种（00，01，10，11），8 比特能表示的状态数是256种

假如用 IP 地址中第 1 段到第 3 段的数值代表公司，用第 4 段的数值代表公司内部的计算机。  
例如，在AAA.BBB.CCC 这个公司内，如果有一台计算机的编号是×××，那么它的IP 地址就是 AAA.BBB.CCC.XXX。   
而看到了 AAA.BBB.CCC.YYY 这样一个 IP 地址，就能知道它是这个公司内的另一台计算机。  

通常把 IP 地址中表示分组（即LAN）的部分称作“网络地址”、表示各台计算机（即主机）的部分称为“主机地址”。  
在上例中，AAA.BBB.CCC 这一部分是网络地址，而 XXX 或 YYY 的部分是主机地址。
 
举例：IP 为 202.26.186.174，子网掩码为 255.255.255.240
- 子网掩码的作用是标识出在 32 比特的 IP 地址中，从哪一位到哪一位是网络地址，从哪一位到哪一位是主机地址。
   - 子网掩码是根据网络设计需求（需要多少子网、每个子网多少主机），通过计算确定网络位和主机位的数量，然后将网络位设为 1、主机位设为 0，最终形成的 32 位二进制数。
- 把 255.255.255.240 用二进制表示的话，结果为 11111111.11111111.11111111.11110000
   - 子网掩码中，值为 1 的那些位对应着 IP 地址中的网络地址，后面值为 0 的那些位则对应着主机地址。
- 因此255.255.255.240 这个子网掩码就表示，其所对应的 IP 地址前 28 比特是网络地址，后 4 比特是主机地址。
   - 例子中IP地址的二进制是11001010.00011010.10111010.10101110，取后四位1110，即主机地址部分是二进制的 1110，也就是十进制的 14
- 4 个二进制数可以表示的范围是从 0000 到 1111，共 16 个数。而因为最开始的 0000 和最后的 1111 具有特殊的用途，所以笔者的办公室内最多可以配置 14 台计算机，它们的主机地址范围是从 0001 到 1110。
   - 这其中又有一台路由器，所以实际上最多只能放置13 台计算机。
> 与 MAC 地址一样，每个 IP 地址的值也都是独一无二的。

## DHCP服务器
IP 地址和子网掩码都是在软件上设置的参数。  
















































































